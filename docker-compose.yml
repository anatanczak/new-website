services:
  app:
    build: .
    container_name: anastasia-website-app
    expose:
      - "3000"

  nginx:
    image: nginx:1.27-alpine
    container_name: anastasia-website-web
    depends_on:
      - app
    ports:
      - "${HOST_PORT}:80"
    volumes:
      - ./nginx/${NGINX_CONF}:/etc/nginx/conf.d/default.conf:ro
